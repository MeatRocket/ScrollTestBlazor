function slide(component: HTMLElement) {
    let slideValue = component.querySelector("slider");
    //component.style.clipPath = "polygon(0 0," + slideValue + "% 0," + slideValue + "% 100%, 0 100%)";
    console.log("Value = " + slideValue);
}
